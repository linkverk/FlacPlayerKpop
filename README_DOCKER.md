# 🐳 K-POP FLAC Player - Docker Edition

## 🚀 Самый быстрый запуск

```bash
# 1. Распакуйте
unzip kpop-flac-player-full-docker.zip
cd kpop-flac-player-full-docker

# 2. Добавьте музыку
mkdir music
cp ~/Music/*.flac music/

# 3. Запустите одной командой
./start.sh

# ИЛИ
docker-compose up -d

# 4. Откройте браузер
# http://localhost:3000
```

**Готово!** 🎉 Frontend и Backend работают в Docker контейнерах!

---

## 📦 Что включено

- ✅ **Docker Compose** конфигурация для обоих сервисов
- ✅ **Dockerfile** для Frontend (Next.js)
- ✅ **Dockerfile** для Backend (ASP.NET Core) 
- ✅ **Makefile** с удобными командами
- ✅ **start.sh** скрипт автоматического запуска
- ✅ **Health checks** для мониторинга
- ✅ **Auto-restart** при падении
- ✅ **Network** изоляция
- ✅ **Полная документация**

---

## 🎯 Основные команды

### С помощью Makefile (рекомендуется)

```bash
make help       # Показать все команды
make up         # Запустить сервисы
make down       # Остановить сервисы
make logs       # Показать логи
make restart    # Перезапустить
make status     # Проверить статус
make build      # Пересобрать образы
```

### С помощью Docker Compose

```bash
docker-compose up -d              # Запустить
docker-compose down               # Остановить
docker-compose logs -f            # Логи
docker-compose ps                 # Статус
docker-compose restart            # Перезапустить
docker-compose up -d --build      # Пересобрать и запустить
```

### С помощью скрипта

```bash
./start.sh      # Автоматический запуск с проверками
```

---

## 🌐 Доступ к сервисам

После запуска:

- **Frontend (UI):** http://localhost:3000
- **Backend (API):** http://localhost:5000
- **API Health:** http://localhost:5000/api/health
- **Music List:** http://localhost:5000/api/music

---

## 📁 Структура проекта

```
kpop-flac-player-full-docker/
├── docker-compose.yml      # 🐳 Главная конфигурация
├── start.sh                # 🚀 Скрипт быстрого запуска
├── Makefile                # 🛠️ Удобные команды
├── .env.example            # ⚙️ Пример конфигурации
├── DOCKER_GUIDE.md         # 📖 Полная документация
│
├── music/                  # 📁 Ваши FLAC файлы ЗДЕСЬ
│   ├── dynamite.flac
│   ├── hylt.flac
│   └── ...
│
├── kpop-backend/           # Backend сервис
│   ├── Dockerfile
│   ├── docker-compose.yml
│   └── ...
│
└── kpop-player/            # Frontend сервис
    ├── Dockerfile
    ├── .dockerignore
    └── ...
```

---

## 🎵 Добавление музыки

Переименуйте ваши FLAC файлы:

```bash
cd music/

# BTS
cp ~/Music/song1.flac dynamite.flac
cp ~/Music/song4.flac butter.flac

# BLACKPINK
cp ~/Music/song2.flac hylt.flac
cp ~/Music/song7.flac pinkvenom.flac

# aespa
cp ~/Music/song3.flac nextlevel.flac
cp ~/Music/song6.flac savage.flac
cp ~/Music/song8.flac spicy.flac

# IVE
cp ~/Music/song5.flac eleven.flac
```

---

## 🔧 Конфигурация

### Изменение портов

Отредактируйте `docker-compose.yml`:

```yaml
services:
  backend:
    ports:
      - "8080:5000"  # Внешний:Внутренний
  
  frontend:
    ports:
      - "8000:3000"
```

### Environment Variables

Скопируйте `.env.example` в `.env` и отредактируйте:

```bash
cp .env.example .env
nano .env
```

---

## 📊 Мониторинг

### Проверка статуса

```bash
# Статус контейнеров
make status
# или
docker-compose ps
```

### Просмотр логов

```bash
# Все логи
make logs

# Следить за логами
make logs-f

# Только backend
make backend-logs

# Только frontend
make frontend-logs
```

### Статистика ресурсов

```bash
# Реалтайм мониторинг
docker stats

# Или через make
make stats
```

---

## 🛠️ Управление контейнерами

### Запуск

```bash
# Первый запуск
./start.sh

# Обычный запуск
make up

# С пересборкой
docker-compose up -d --build
```

### Остановка

```bash
# Остановить (сохранить данные)
make down

# Остановить и удалить volumes
docker-compose down -v
```

### Перезапуск

```bash
# Перезапустить всё
make restart

# Только backend
docker-compose restart backend

# Только frontend
docker-compose restart frontend
```

### Обновление

```bash
# Обновить код и перезапустить
make rebuild

# Или вручную
docker-compose down
docker-compose up -d --build
```

---

## 🐛 Troubleshooting

### Контейнеры не запускаются

```bash
# Проверьте логи
docker-compose logs

# Проверьте статус
docker-compose ps

# Пересоберите
docker-compose up -d --build
```

### Порт занят

```bash
# Найдите процесс
lsof -i :3000  # macOS/Linux
netstat -ano | findstr :3000  # Windows

# Или измените порт в docker-compose.yml
```

### Музыка не играет

```bash
# Проверьте файлы
ls -la music/

# Проверьте права
chmod -R 755 music/

# Проверьте внутри контейнера
docker-compose exec backend ls -la /app/music
```

### Очистка Docker

```bash
# Удалить всё
make clean

# Или вручную
docker-compose down -v
docker system prune -a
```

---

## 🚀 Production Deployment

### С SSL/TLS

Добавьте Nginx reverse proxy. Пример в `DOCKER_GUIDE.md`.

### На VPS/Cloud

```bash
# 1. Загрузите проект на сервер
scp kpop-flac-player-full-docker.zip user@server:~/

# 2. Распакуйте
ssh user@server
unzip kpop-flac-player-full-docker.zip

# 3. Добавьте музыку
mkdir music && cp /path/to/music/*.flac music/

# 4. Запустите
./start.sh

# 5. Настройте firewall
sudo ufw allow 3000
sudo ufw allow 5000
```

---

## 📚 Документация

- **DOCKER_GUIDE.md** - Полное руководство по Docker
- **INTEGRATION_GUIDE.md** - Интеграция Frontend + Backend
- **QUICKSTART.md** - Быстрый старт без Docker
- **README_ARCHIVES.md** - Описание всех архивов
- **kpop-backend/README.md** - API документация
- **kpop-player/README.md** - Frontend документация

---

## ⚡ Преимущества Docker версии

- ✅ **Изолированная среда** - не влияет на систему
- ✅ **Легкое развертывание** - одна команда для запуска
- ✅ **Портативность** - работает везде где есть Docker
- ✅ **Масштабируемость** - легко добавить реплики
- ✅ **Автовосстановление** - контейнеры перезапускаются при падении
- ✅ **Изоляция сети** - безопасность из коробки
- ✅ **Health checks** - автоматический мониторинг

---

## 🎯 Сравнение: Docker vs Обычный запуск

| Особенность | Docker | Обычный запуск |
|-------------|--------|----------------|
| Установка зависимостей | ❌ Не нужна | ✅ Нужна (.NET + Node.js) |
| Запуск | `docker-compose up` | 2 команды в 2 терминалах |
| Изоляция | ✅ Полная | ❌ Нет |
| Портативность | ✅ Да | ⚠️ Требует настройки |
| Production | ✅ Готов | ⚠️ Требует настройки |
| Разработка | ⚠️ Немного медленнее | ✅ Быстрее |

**Рекомендация:** Docker для production, обычный запуск для разработки.

---

## 💡 Полезные советы

1. **Используйте `make help`** для списка всех команд
2. **`./start.sh`** проверит всё автоматически
3. **Логи в реальном времени:** `make logs-f`
4. **Быстрая перезагрузка:** `make restart`
5. **Полная пересборка:** `make rebuild`

---

## 🎉 Готово!

Теперь у вас есть полностью контейнеризованный K-POP плеер!

```bash
# Запустите
./start.sh

# Откройте
http://localhost:3000

# Наслаждайтесь музыкой! 🎵
```

---

**Made with ❤️ for K-POP and Docker lovers**

Для подробной информации смотрите `DOCKER_GUIDE.md`
