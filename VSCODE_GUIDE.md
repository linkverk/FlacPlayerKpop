# 🚀 VS Code - Запуск и Отладка K-POP FLAC Player

## ⚡ Быстрый запуск в VS Code

### Открыть проект

```bash
# Распакуйте и откройте workspace
unzip kpop-flac-player-full-docker.zip
cd kpop-flac-player-full-docker
code kpop-flac-player.code-workspace
```

---

## 🎯 Способы запуска

### 1️⃣ Docker (рекомендуется) - Одна кнопка

**Через VS Code Tasks:**

1. Нажмите `Ctrl+Shift+P` (Cmd+Shift+P на Mac)
2. Введите "Tasks: Run Task"
3. Выберите **"🚀 Start All (Docker)"**

**Или через терминал:**
```bash
# Ctrl+` для открытия терминала
docker-compose up -d
```

✅ **Готово!** Backend на :5000, Frontend на :3000

---

### 2️⃣ Запуск через Debug Panel (F5)

**Шаг 1:** Откройте Debug panel (Ctrl+Shift+D)

**Шаг 2:** Выберите конфигурацию:

#### Вариант A: Запустить оба сервиса
- Выберите **"🎵 Start Frontend + Backend"**
- Нажмите **F5** или зеленую кнопку ▶️

#### Вариант B: Только Frontend
- Выберите **"🎨 Frontend (Next.js)"**
- Нажмите **F5**

#### Вариант C: Только Backend
- Выберите **"⚙️ Backend (ASP.NET)"**
- Нажмите **F5**

#### Вариант D: Docker
- Выберите **"🚀 Full Stack (Both)"**
- Нажмите **F5**

---

### 3️⃣ Запуск через Tasks

**Открыть Tasks:**
- `Ctrl+Shift+P` → "Tasks: Run Task"

**Доступные задачи:**

| Task | Описание | Горячая клавиша |
|------|----------|-----------------|
| 🚀 Start All (Docker) | Запустить в Docker | Ctrl+Shift+B |
| 🛑 Stop All (Docker) | Остановить Docker | - |
| 🔄 Restart All (Docker) | Перезапустить Docker | - |
| 📋 View Logs (Docker) | Логи Docker | - |
| ▶️ Frontend Dev | Запустить Frontend | - |
| ⚙️ Backend Dev | Запустить Backend | - |
| 🔨 Build Frontend | Собрать Frontend | - |
| 🔨 Build Backend | Собрать Backend | - |
| 🧪 Install Frontend | npm install | - |
| 🧪 Restore Backend | dotnet restore | - |

---

### 4️⃣ Запуск через Встроенный Терминал

**Открыть терминал:** `Ctrl+`` (или View → Terminal)

#### Docker (самый простой):
```bash
# Запустить всё
docker-compose up -d

# Или через Makefile
make up

# Или через скрипт
./start.sh
```

#### Без Docker:

**Терминал 1 - Backend:**
```bash
cd kpop-backend
dotnet run
```

**Терминал 2 - Frontend:**
```bash
cd kpop-player
npm install  # первый раз
npm run dev
```

---

## 🐛 Отладка (Debugging)

### Frontend (Next.js)

**1. Запустить с отладчиком:**
- Откройте Debug panel (Ctrl+Shift+D)
- Выберите **"Next.js: debug full stack"**
- Нажмите **F5**
- Установите breakpoints в коде (клик слева от номера строки)

**2. Debug в браузере:**
- Установите breakpoint в файле (например, `app/page.tsx`)
- Запустите **"Next.js: debug client-side"**
- VS Code откроет Chrome с отладчиком

**3. Debug server-side:**
- Выберите **"Next.js: debug server-side"**
- Отладка API routes и серверного кода

### Backend (ASP.NET Core)

**1. Запустить с отладчиком:**
- Откройте Debug panel
- Выберите **".NET Core Launch (web)"**
- Нажмите **F5**
- Установите breakpoints в `Program.cs`

**2. Attach к процессу:**
- Если backend уже запущен
- Выберите **".NET Core Attach"**
- Выберите процесс `KPopFlacMusicServer`

**3. Hot Reload:**
```bash
# В терминале backend
dotnet watch run
```
Изменения применяются автоматически!

### Docker Debug

VS Code автоматически подключается к контейнерам.

**Просмотр логов:**
```bash
# В терминале
docker-compose logs -f

# Или через Task
Ctrl+Shift+P → "Tasks: Run Task" → "📋 View Logs (Docker)"
```

---

## 🎨 Интерфейс VS Code

### Панели

```
┌─────────────────────────────────────────┐
│  Activity Bar  │  Side Bar  │  Editor   │
│  (слева)       │  (боковая) │  (центр)  │
├─────────────────────────────────────────┤
│                 Terminal                 │
│              Status Bar                  │
└─────────────────────────────────────────┘
```

**Activity Bar (слева):**
- 📁 Explorer (Ctrl+Shift+E) - файлы
- 🔍 Search (Ctrl+Shift+F) - поиск
- 🔧 Source Control (Ctrl+Shift+G) - Git
- 🐛 Run and Debug (Ctrl+Shift+D) - отладка
- 📦 Extensions (Ctrl+Shift+X) - расширения
- 🐳 Docker - контейнеры

### Debug Panel

**Открыть:** `Ctrl+Shift+D` или нажмите 🐛 в Activity Bar

**Элементы:**
1. **Конфигурация** (dropdown) - выбор режима запуска
2. **Кнопка запуска** (▶️) - запустить с отладкой
3. **Variables** - переменные
4. **Watch** - отслеживание
5. **Call Stack** - стек вызовов
6. **Breakpoints** - точки останова

**Управление:**
- **F5** - Запустить/Продолжить
- **F10** - Step Over (перешагнуть)
- **F11** - Step Into (войти в функцию)
- **Shift+F11** - Step Out (выйти из функции)
- **Shift+F5** - Остановить

---

## 📋 Полезные сочетания клавиш

### Общие

| Действие | Windows/Linux | macOS |
|----------|---------------|-------|
| Command Palette | Ctrl+Shift+P | Cmd+Shift+P |
| Quick Open | Ctrl+P | Cmd+P |
| Terminal | Ctrl+` | Cmd+` |
| Боковая панель | Ctrl+B | Cmd+B |
| Поиск в файлах | Ctrl+Shift+F | Cmd+Shift+F |
| Сохранить все | Ctrl+K S | Cmd+K S |

### Редактирование

| Действие | Windows/Linux | macOS |
|----------|---------------|-------|
| Форматировать | Shift+Alt+F | Shift+Option+F |
| Комментарий | Ctrl+/ | Cmd+/ |
| Дублировать строку | Shift+Alt+Down | Shift+Option+Down |
| Удалить строку | Ctrl+Shift+K | Cmd+Shift+K |
| Перейти к определению | F12 | F12 |
| Найти все ссылки | Shift+F12 | Shift+F12 |

### Отладка

| Действие | Клавиша |
|----------|---------|
| Запустить/Продолжить | F5 |
| Остановить | Shift+F5 |
| Перезапустить | Ctrl+Shift+F5 |
| Step Over | F10 |
| Step Into | F11 |
| Step Out | Shift+F11 |
| Toggle Breakpoint | F9 |

### Терминал

| Действие | Windows/Linux | macOS |
|----------|---------------|-------|
| Новый терминал | Ctrl+Shift+` | Cmd+Shift+` |
| Разделить | Ctrl+Shift+5 | Cmd+Shift+5 |
| Переключить | Alt+Left/Right | Cmd+Left/Right |
| Очистить | Ctrl+K | Cmd+K |

---

## 🔧 Настройка горячих клавиш

**Открыть настройки клавиш:**
- `Ctrl+K Ctrl+S`
- Или: File → Preferences → Keyboard Shortcuts

**Полезные настройки:**

```json
// keybindings.json
[
  {
    "key": "ctrl+shift+d",
    "command": "workbench.action.tasks.runTask",
    "args": "🚀 Start All (Docker)"
  },
  {
    "key": "ctrl+shift+x",
    "command": "workbench.action.tasks.runTask",
    "args": "🛑 Stop All (Docker)"
  }
]
```

---

## 📊 Работа с терминалами

### Создать Split Terminal

**Способ 1:** Через UI
- Нажмите иконку разделения в терминале
- Или: Ctrl+Shift+5

**Способ 2:** Через задачи
```json
{
  "label": "Start Both Services",
  "dependsOn": ["Frontend Dev", "Backend Dev"],
  "problemMatcher": []
}
```

### Профили терминалов

**Создать профили:**
1. Ctrl+Shift+P → "Terminal: Select Default Profile"
2. Выберите bash/zsh/powershell

**Использование:**
- Терминал 1: Frontend (npm run dev)
- Терминал 2: Backend (dotnet run)
- Терминал 3: Docker (docker-compose logs -f)

---

## 🐳 Docker в VS Code

### Docker Extension

**Установить:** 
- Ctrl+Shift+X
- Найти "Docker"
- Установить от Microsoft

**Возможности:**
- 📦 Просмотр контейнеров
- 📊 Просмотр образов
- 🌐 Просмотр сетей
- 💾 Просмотр volumes
- ▶️ Запуск/остановка одним кликом
- 📋 Просмотр логов
- 💻 Exec в контейнер

**Использование:**

1. Нажмите иконку 🐳 в Activity Bar
2. Разверните **Containers**
3. ПКМ на контейнере → опции:
   - View Logs
   - Attach Shell
   - Restart
   - Stop
   - Remove

### Быстрые действия

**Запустить Docker Compose:**
- ПКМ на `docker-compose.yml` → "Compose Up"

**Остановить:**
- ПКМ на `docker-compose.yml` → "Compose Down"

**Логи:**
- Containers → ПКМ → View Logs

---

## 🎯 Рабочий процесс (Workflow)

### Первый запуск

1. **Откройте workspace:**
   ```bash
   code kpop-flac-player.code-workspace
   ```

2. **Установите расширения:**
   - VS Code предложит автоматически
   - Или: Ctrl+Shift+X → Установить рекомендуемые

3. **Добавьте музыку:**
   ```bash
   mkdir music
   cp ~/Music/*.flac music/
   ```

4. **Запустите:**
   - `Ctrl+Shift+B` (Docker)
   - Или F5 → выберите конфигурацию

### Ежедневная разработка

**С Docker:**
```bash
# Утром
make up

# Работа (hot reload включен)
# Редактируйте код - изменения применяются автоматически

# Вечером
make down
```

**Без Docker (более быстрый hot reload):**

**Terminal 1:**
```bash
cd kpop-backend
dotnet watch run
```

**Terminal 2:**
```bash
cd kpop-player
npm run dev
```

Оба сервиса с автоперезагрузкой!

---

## 🔍 Debugging Tips

### Frontend

**Breakpoints в React компонентах:**
```typescript
// components/AudioPlayer.tsx
const togglePlay = async () => {
  debugger; // 👈 или кликните слева от строки
  if (!audioRef.current) return;
  // ...
}
```

**Console в компонентах:**
```typescript
useEffect(() => {
  console.log('Current track:', currentTrack);
}, [currentTrack]);
```

**React DevTools:**
- Установите расширение в Chrome
- Инспектируйте компоненты

### Backend

**Breakpoints в API:**
```csharp
app.MapGet("/api/music", () =>
{
    // Установите breakpoint здесь 👈
    var availableTracks = musicLibrary.Where(track =>
    {
        var filepath = Path.Combine(musicPath, track.filename);
        return File.Exists(filepath);
    }).ToList();
    
    return Results.Ok(new { tracks = musicLibrary });
});
```

**Console logging:**
```csharp
Console.WriteLine($"Found {availableCount} tracks");
```

---

## 🎨 Настройка UI

### Theme

**Сменить тему:**
- Ctrl+K Ctrl+T
- Или: Preferences → Color Theme

**Популярные темы:**
- One Dark Pro
- Dracula Official
- Material Theme
- GitHub Theme

### Font

**Рекомендуемые шрифты:**
- Fira Code (с лигатурами)
- JetBrains Mono
- Cascadia Code

**Настройка:**
```json
{
  "editor.fontFamily": "Fira Code",
  "editor.fontLigatures": true,
  "editor.fontSize": 14
}
```

---

## 📦 Полезные расширения

### Обязательные (уже в рекомендациях)

- ✅ **ESLint** - линтинг JS/TS
- ✅ **Prettier** - форматирование
- ✅ **Tailwind CSS IntelliSense** - автодополнение CSS
- ✅ **C# Dev Kit** - разработка на C#
- ✅ **Docker** - работа с контейнерами

### Дополнительные (опционально)

- 🎨 **Better Comments** - цветные комментарии
- 📊 **GitLens** - расширенный Git
- 🔍 **Error Lens** - ошибки inline
- 📝 **Auto Rename Tag** - переименование парных тегов
- 🎯 **Path Intellisense** - автодополнение путей
- 💰 **Import Cost** - размер импортов
- 🎪 **Peacock** - цветовое кодирование workspace
- 📸 **Polacode** - скриншоты кода

---

## 🚨 Troubleshooting

### Расширения не устанавливаются

```bash
# Переустановите VS Code
# Или очистите кеш
rm -rf ~/.vscode/extensions
```

### Debug не работает

1. Проверьте `.vscode/launch.json`
2. Убедитесь что порты свободны
3. Перезапустите VS Code

### Hot Reload не работает

**Frontend:**
```bash
# Очистите .next
rm -rf .next
npm run dev
```

**Backend:**
```bash
# Используйте watch
dotnet watch run
```

### Терминал не открывается

- Ctrl+Shift+P → "Terminal: Create New Terminal"
- Или перезапустите VS Code

---

## 💡 Pro Tips

1. **Multi-cursor editing:**
   - Alt+Click для добавления курсора
   - Ctrl+D для выбора следующего вхождения

2. **Zen Mode:**
   - Ctrl+K Z для фокусировки
   - Esc Esc для выхода

3. **Split Editor:**
   - Ctrl+\ для разделения
   - Ctrl+1/2/3 для переключения

4. **Quick Fix:**
   - Ctrl+. на проблеме
   - Быстрые исправления

5. **Rename Symbol:**
   - F2 на переменной/функции
   - Переименует везде

---

## 📚 Дополнительные ресурсы

- [VS Code Documentation](https://code.visualstudio.com/docs)
- [Debugging in VS Code](https://code.visualstudio.com/docs/editor/debugging)
- [VS Code Tips and Tricks](https://code.visualstudio.com/docs/getstarted/tips-and-tricks)
- [Docker in VS Code](https://code.visualstudio.com/docs/containers/overview)

---

## 🎉 Готово!

Теперь вы знаете как:
- ✅ Запускать проект разными способами
- ✅ Отлаживать Frontend и Backend
- ✅ Работать с Docker в VS Code
- ✅ Использовать горячие клавиши
- ✅ Настроить удобный рабочий процесс

**Начните разработку:**
```bash
# Откройте проект
code kpop-flac-player.code-workspace

# Запустите (Ctrl+Shift+B)
# Или F5 для отладки
```

Наслаждайтесь разработкой! 🎵💻
