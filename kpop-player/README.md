# 🎵 K-POP FLAC Player - Next.js

Современный высококачественный музыкальный плеер для K-POP треков в формате FLAC, построенный на Next.js 14 с TypeScript и Framer Motion.

## ✨ Возможности

- 🎧 **Hi-Res Audio**: Воспроизведение FLAC в качестве 24bit/96kHz
- 🎨 **Cosmic Design**: Яркий дизайн с неоновыми акцентами и голографическими эффектами
- 🌊 **Streaming**: Потоковое воспроизведение с поддержкой Range requests
- ⚡ **Next.js 14**: Современный стек с App Router и API Routes
- 🎭 **Анимации**: Плавные переходы с Framer Motion
- 📱 **Responsive**: Адаптивный дизайн для всех устройств
- 🎚️ **Полный контроль**: Play, pause, next, previous, volume, seeking
- 📊 **Визуализатор**: Анимированный аудио визуализатор
- 🔄 **Автоплей**: Автоматическая смена треков

## 🚀 Быстрый старт

### Установка

```bash
# Установите зависимости
npm install

# Запустите dev сервер
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

### Добавление музыки

1. Создайте папку `public/music/` в корне проекта
2. Добавьте туда ваши FLAC файлы
3. Переименуйте файлы согласно списку в `app/api/music/route.ts`:
   - `dynamite.flac`
   - `hylt.flac`
   - `nextlevel.flac`
   - `butter.flac`
   - `eleven.flac`
   - `savage.flac`
   - `pinkvenom.flac`
   - `spicy.flac`

4. Обновите страницу

## 📁 Структура проекта

```
kpop-player/
├── app/
│   ├── api/
│   │   ├── music/
│   │   │   └── route.ts          # API для списка треков
│   │   └── stream/
│   │       └── [filename]/
│   │           └── route.ts      # API для стриминга аудио
│   ├── globals.css               # Глобальные стили и анимации
│   ├── layout.tsx                # Root layout
│   └── page.tsx                  # Главная страница
├── components/
│   ├── AudioPlayer.tsx           # Компонент плеера
│   ├── TrackCard.tsx             # Карточка трека
│   └── ServerStatus.tsx          # Статус подключения
├── types/
│   └── index.ts                  # TypeScript типы
├── public/
│   └── music/                    # 📁 Папка для FLAC файлов
├── package.json
├── tsconfig.json
├── tailwind.config.js
├── next.config.js
└── README.md
```

## 🎨 Технологии

- **Next.js 14** - React фреймворк с App Router
- **TypeScript** - Типизация
- **Tailwind CSS** - Утилитарный CSS
- **Framer Motion** - Анимации
- **Lucide React** - Иконки
- **Node.js File Streaming** - Потоковая передача аудио

## 🎮 Использование

### Основные возможности:

1. **Выбор трека**: Кликните на карточку трека
2. **Воспроизведение**: Кнопка Play/Pause в центре плеера
3. **Навигация**: Previous/Next для переключения треков
4. **Перемотка**: Клик по progress bar
5. **Громкость**: Слайдер громкости (desktop)
6. **Автоплей**: Автоматически переключается на следующий трек

### Горячие клавиши:

Плеер реагирует на нажатия кнопок на карточках и в плеере.

## 🔧 Настройка библиотеки

Отредактируйте файл `app/api/music/route.ts` для добавления новых треков:

```typescript
const musicLibrary = [
  {
    id: 9,
    title: "Ваша песня",
    artist: "Исполнитель",
    filename: "your-song.flac",
    format: "FLAC 24bit/96kHz",
    emoji: "🎤"
  },
  // ... другие треки
];
```

## 🎨 Кастомизация дизайна

### Цвета

Измените цвета в `tailwind.config.js`:

```javascript
colors: {
  neon: {
    pink: '#FF10F0',    // Неоновый розовый
    cyan: '#00F0FF',    // Неоновый голубой
    purple: '#B724FF',  // Неоновый фиолетовый
    yellow: '#FFF500',  // Неоновый желтый
  },
}
```

### Шрифты

Измените шрифты в `app/globals.css`:

```css
@import url('https://fonts.googleapis.com/css2?family=YourFont&display=swap');

:root {
  --font-display: 'YourFont', sans-serif;
  --font-body: 'YourBodyFont', sans-serif;
}
```

### Анимации

Все анимации настраиваются в `app/globals.css` и используют CSS keyframes для производительности.

## 🌐 API Endpoints

### GET `/api/music`
Возвращает список всех треков из библиотеки.

**Response:**
```json
{
  "success": true,
  "tracks": [...],
  "availableCount": 8,
  "totalCount": 8
}
```

### GET `/api/stream/[filename]`
Стримит аудио файл с поддержкой Range requests (для перемотки).

**Headers:**
- `Range: bytes=0-1024` - для частичной загрузки
- `Content-Type: audio/flac`
- `Accept-Ranges: bytes`

## 📱 Адаптивность

Плеер полностью адаптивен:
- **Mobile**: Упрощенный UI, вертикальная ориентация
- **Tablet**: Сеточный layout 2 колонки
- **Desktop**: Полный UI с 4 колонками треков

## ⚡ Производительность

- **Lazy Loading**: Компоненты загружаются по мере необходимости
- **Streaming**: Файлы стримятся, а не загружаются полностью
- **CSS Animations**: Используются CSS вместо JS для плавности
- **Optimized Images**: Эмодзи вместо изображений для скорости

## 🐛 Troubleshooting

### Треки не воспроизводятся
- ✅ Проверьте, что FLAC файлы в `public/music/`
- ✅ Проверьте имена файлов (должны совпадать с API)
- ✅ Откройте DevTools (F12) и проверьте консоль на ошибки

### Плеер не загружается
- ✅ Убедитесь, что установлены зависимости: `npm install`
- ✅ Проверьте версию Node.js: `node --version` (требуется 18+)
- ✅ Очистите кеш: `rm -rf .next && npm run dev`

### Плохое качество звука
- ✅ Убедитесь, что используете FLAC файлы (не MP3)
- ✅ Проверьте битрейт ваших файлов
- ✅ Используйте качественные наушники/динамики

## 🚀 Production Build

```bash
# Создать production build
npm run build

# Запустить production сервер
npm start
```

## 🔒 Безопасность

- ✅ Path validation предотвращает directory traversal
- ✅ CORS настроен правильно для локального использования
- ✅ Только разрешенные форматы файлов
- ✅ Rate limiting на API endpoints (добавить при деплое)

## 📄 Лицензия

MIT License

## 🎵 Enjoy Your Music!

Создано с ❤️ для любителей K-POP и высококачественного аудио.

---

### Полезные ссылки:
- [Next.js Documentation](https://nextjs.org/docs)
- [Framer Motion](https://www.framer.com/motion/)
- [Tailwind CSS](https://tailwindcss.com/)
